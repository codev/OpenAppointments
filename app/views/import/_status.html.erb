<%# Analyze summary or the running import's status; polls while it runs. Locals: import_status, summary. %>
<% status = import_status&.with_indifferent_access %>
<% running = status && %w[queued running].include?(status[:state]) %>
<turbo-frame id="import-status" <% if running %>data-poll-every="2000" data-poll-src="/data?import_id=<%= params[:import_id] %>"<% end %>>
    <% if summary %>
        <div id="import-results" class="notice" data-tone="info" style="white-space: pre-line;"><%= summary %></div>
    <% elsif alert %>
        <div id="import-results" class="notice" data-tone="error"><%= alert %></div>
    <% elsif status %>
        <% case status[:state] %>
        <% when "completed" %>
            <% errors = Array(status[:errors]) %>
            <div id="import-results" class="notice" data-tone="<%= errors.any? ? 'warning' : 'success' %>" style="white-space: pre-line;"><%= lang('import_complete') %>
<% (status[:counts] || {}).each do |phase, entry| %><%= lang(phase) %>: <%= entry[:created] %> <%= lang('created') %>, <%= entry[:matched] %> <%= lang('matched') %>, <%= entry[:skipped] %> <%= lang('skipped') %><%= ", #{entry[:failed]} #{lang('failed')}" if entry[:failed].to_i.positive? %>
<% end %><% if errors.any? %>
<%= lang('import_failures') %>
<% errors.each do |error| %><%= lang(error[:phase]) %>: <%= error[:item] %> - <%= error[:message] %>
<% end %><% end %></div>
        <% when "failed" %>
            <div id="import-results" class="notice" data-tone="error"><%= status[:error].presence || lang('unexpected_issues_occurred') %></div>
        <% else %>
            <div id="import-results" class="notice" data-tone="info"><%= lang('import_running') %><%= " (#{lang(status[:phase])})" if status[:phase].present? %></div>
        <% end %>
    <% end %>
</turbo-frame>
