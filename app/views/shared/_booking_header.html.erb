<%# Port of EA components/booking_header.php. Locals: company_name, company_logo.
    The header sits outside the wizard frame: it renders the current step here and
    pages/booking.js mirrors #wizard-state into it on every frame load. %>
<header id="header" class="wizard-header">
    <div id="company-name" class="company">
        <img src="<%= company_logo.presence || image_path("logo.png") %>" alt="" id="company-logo">
        <span><%= company_name %></span>
        <span class="display-booking-selection"><%= wizard_selection_text %></span>
    </div>

    <%
      selection_titles = vars(:first_step) == "provider" ? %w[select_provider select_service] : %w[select_service select_provider]
      step_titles = selection_titles + %w[appointment_date_and_time customer_information appointment_confirmation]
      links = wizard_step_links
    %>
    <ol id="steps" class="wizard-steps">
        <% step_titles.each_with_index do |title, index| %>
            <% number = index + 1 %>
            <% next unless wizard_header_steps.cover?(number) %>
            <li id="step-<%= number %>" data-step-index="<%= number %>"
                class="book-step <%= 'active-step' if number == step_number %> <%= 'completed-step' if links[number] %>"
                data-tippy-content="<%= lang(title) %>" aria-label="<%= lang(title) %>"
                <%= 'aria-current="step"'.html_safe if number == step_number %>
                <% if links[number] %>role="button" tabindex="0" data-href="<%= links[number] %>"<% end %>>
                <strong>
                    <span class="step-number"><%= number - wizard_header_steps.first + 1 %></span>
                    <i class="fas fa-check step-check" aria-hidden="true"></i>
                </strong>
            </li>
        <% end %>
    </ol>
</header>
