<div class="integration-status">
    <% if on %>
        <span>&#9989;</span><%= text || lang('active') %>
    <% else %>
        <span class="muted"><%= text || lang('not_active') %></span>
    <% end %>
</div>
