<div class="integration-status small mb-2">
    <% if on %>
        <span class="me-1">&#9989;</span><%= text || lang('active') %>
    <% else %>
        <span class="text-muted"><%= text || lang('not_active') %></span>
    <% end %>
</div>
